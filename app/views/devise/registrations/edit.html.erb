<main>
  <div class="container">
    <div class="text-center my-3">
      <h2>ユーザー詳細</h2>
    </div>
    <div class="login-form">
      <h3>ユーザー情報更新</h3>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-group">
          <%= f.label :username %>
          <%= f.text_field :username, autofocus: true, autocomplete: "username", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div class="mb-3">現在、新しいアドレスの確認を待っています: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="form-group">
          <%= f.label "新しいパスワード" %> <i>(変更したくない場合は空欄にしてください)</i>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control"  %>
          <% if @minimum_password_length %>
            <br />
            <em><%= @minimum_password_length %>文字以上</em>
          <% end %>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation %> <i>(変更したくない場合は空欄にしてください)</i><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control"  %>
        </div>

        <div class="form-group">
          <%= f.label :current_password %> <i>(ユーザー情報を変更するには現在のパスワードが必要です)</i><br />
          <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control"  %>
        </div>

        <div class="actions">
          <%= f.submit "更新", class: "btn btn-primary btn-raised" %>
        </div>
      <% end %>

      <h3>アカウント削除</h3>

      <p>一度アカウントを削除すると、二度と元に戻せません。十分ご注意ください。 <%= button_to "削除", registration_path(resource_name), data: { confirm: "カードの情報は残ります。カードを消したい場合は個別に削除してください。" }, method: :delete, class: "btn btn-danger btn-raised" %></p>

      <%= link_to "戻る", :back %>

    </div>
  </div>
</main>